# Required to enable MongoDB persistence. If unset, the bot logs only to files.
MONGODB_URI=
# Required when MONGODB_URI is set. If unset while URI is set, Mongo persistence is disabled.
MONGODB_DB_NAME=
# Optional. Defaults to "submissions" when unset.
MONGODB_COLLECTION_NAME=
